# Care Sync - Root requirements.txt
# NOTE: This file is for local development only
# Vercel uses api/requirements.txt for serverless functions
# 
# For local development, install with: pip3 install -r requirements.txt
# This includes FastAPI/uvicorn for local testing (not used in Vercel)

# FastAPI web framework (for local dev only, not used in Vercel)
fastapi==0.104.1

# ASGI server for FastAPI (for local dev only, not used in Vercel)
uvicorn[standard]==0.24.0

# Data validation using Python type annotations
pydantic>=2.8.0

# OpenAI API client for GPT fact extraction
openai==1.3.5

# Environment variable management
python-dotenv==1.0.0

# Multipart form data support
python-multipart==0.0.6

# HTTP client for async requests
httpx==0.25.2
